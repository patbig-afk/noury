# Base Postgres (Neon via le Marketplace Vercel : les 2 variables sont créées automatiquement)
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"
DATABASE_URL_UNPOOLED="postgresql://user:password@host/db?sslmode=require"

# Store Vercel Blob (créé en mode PRIVÉ, connexion OIDC : variables créées automatiquement)
BLOB_STORE_ID=""
BLOB_WEBHOOK_PUBLIC_KEY=""
# Ancien mode par jeton (accepté à la place de l'OIDC)
# BLOB_READ_WRITE_TOKEN=""
